(() => {

  let productTitle = 'My amazing product';
  productTitle = 'another product';
  console.log(productTitle);

  const description = "asdadas";
  console.log(description);

  let summary = `
    title: ${productTitle}
    description: ${description}
  `;
  console.log(summary);

})()